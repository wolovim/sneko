[project]
name = "sneko"
version = "0.0.3"
description = "Terminal GUI for Ethereum smart contracts"
authors = [
    { name = "Marc Garreau", email = "marc@ethereum.org" }
]
dependencies = [
    "py-solc-x",
    "pyperclip",
    "textual",
    "titanoboa==0.1.10b1",
    # "titanoboa>=0.1.9",
    # "titanoboa@git+https://github.com/vyperlang/titanoboa#egg=master",
]
readme = "README.md"
requires-python = ">= 3.8"

[project.scripts]
"sneko" = "sneko:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.rye]
managed = true
dev-dependencies = [
    "textual-dev"
]

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["src/sneko"]
